#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _puts_recursion - prints a string, followed by a new line, to stdout
 * @s: string to print
 */
void _puts_recursion(char *s)
{
puts(s);
}
